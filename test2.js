// let name = 'bob';
// console.log('hi ${name}');
/*
function reason(){return 1;}

function reason() {
    return 1;
  }     
  let inter = `give me ${reason() === 1 ? "one good reason" : "a few reasons"} to try this`;
  console.log(inter);
  */

  let pattern = /xyz/;
  let value = "this is xyz a test";

  console.log(value.replace(pattern, 'just'));